# AWS Terraform Skeleton (Optional)

Placeholder IaC folder. Typical components:
- ECS/Fargate (UI + API)
- ALB + HTTPS
- CloudWatch logs
- Secrets Manager for API keys
- (Optional) S3 for docs + embedding artifacts
